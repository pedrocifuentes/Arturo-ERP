{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BackSizes list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Active</th>
                <th>Sort</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('backsizes_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.active }}</td>
                <td>{{ entity.sort }}</td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('backsizes_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backsizes_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('backsizes_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
