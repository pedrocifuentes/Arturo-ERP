{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ProdPricesDetail creation</h1>

    <form action="{{ path('prodpricesdetail_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('prodpricesdetail') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
