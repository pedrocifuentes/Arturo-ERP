{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FosUserUser creation</h1>

    <form action="{{ path('fosuseruser_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('fosuseruser') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
