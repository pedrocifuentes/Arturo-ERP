{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BackCountries list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Code</th>
                <th>Latitude</th>
                <th>Longitude</th>
                <th>Address</th>
                <th>Zoom</th>
                <th>Catalog</th>
                <th>Email</th>
                <th>Active</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('backcountries_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.code }}</td>
                <td>{{ entity.latitude }}</td>
                <td>{{ entity.longitude }}</td>
                <td>{{ entity.address }}</td>
                <td>{{ entity.zoom }}</td>
                <td>{{ entity.catalog }}</td>
                <td>{{ entity.email }}</td>
                <td>{{ entity.active }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('backcountries_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('backcountries_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('backcountries_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
