{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Combination creation</h1>

    <form action="{{ path('combination_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('combination') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
